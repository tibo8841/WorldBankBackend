// export default function postSearchHistory(userID, search) {
//   await clientUser.queryArray(
//     `INSERT INTO search_history (user_id, countries, year_range, created_at) VALUES (?, ?, ?,NOW());`,[userID,search.countries,search.yearRange]
//   );
// }
